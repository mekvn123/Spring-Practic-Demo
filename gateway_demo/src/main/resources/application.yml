server:
  port: 8080
spring:
  application:
    name: gateway_demo
  cloud:
    gateway:
      enabled: true
      routes:
        - id: main_application_demo
          uri: http://localhost:8081
          order: 1
          predicates:
            - Path=/main_application_demo/**
          filters:
            - Auth=true